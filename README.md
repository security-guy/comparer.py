# comparer.py

Takes two files as command line arugments, txt and/or CSV. Outputs two separate files, one matching items, another non matching items.

CSV output may be a little wonky as a new line is created with each iteration.
